function y=decimate2(x,r)% DECIMATE2 - performs a reduced sampling of the input vector x%	by a factor of r.  Unlike the MATLAB function called decimate,%	this function does not perform any pre-filtering on the%	original waveform x.  The decimation is performed such that%	y(1) = x(1).%if nargin < 2   error('Not enough input arguments.')endif abs(r-fix(r)) > eps   error('Resampling rate R must be a positive integer.')endif fix(r) == 1	y = x;	returnendif r <= 0	error('Resampling rate R must be a positive integer.')endlist=1:r:max(size(x));y=x(list);